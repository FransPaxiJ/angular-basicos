import { Component } from '@angular/core';

@Component({
  selector: 'app-root', //in the index.html file, we will use this selector to reference the component
  templateUrl: './app.component.html', //this is file that will be used to display the component
})
export class AppComponent {
  title = 'bases';

}
